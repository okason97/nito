<%= form_with(model: test_course, local: true) do |form| %>
  <% if test_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_course.errors.count, "error") %> prohibited this test_course from being saved:</h2>

      <ul>
      <% test_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
