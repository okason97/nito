# Como usar la aplicacion

Para usar la aplicacion:

* bundle install

* rake db:migrate

* rake db:setup

* bundle exec rails s

Para reiniciar la db:

* rake db:drop

* rake db:migrate

* rake db:setup